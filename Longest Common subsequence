#include <iostream>
using namespace std;

int main() {
    string arr2[n];
    string arr[m];
    int lcs[n][m];
	for(int i=0;i<n;i++){
	    for(j=0;j<m;j++){
	        if(i==0||j==0){
	            lcs[i][j] = 0;
	        }else if(arr2[i]==arr[j]){
	            lcs[i][j] = 1+ lcs[i-1][j-1];
	        }else{
	            lcs[i][j] = max(lcs[i][j-1],lcs[i-1][j]);
	        }
	    }
	}
}
